{
  "name": "indotube",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "backend:build": "pnpm --stream -r build",
    "db:gen": "npx drizzle-kit generate",
    "db:migrate": "npx drizzle-kit migrate",
    "dev": "pnpm --stream -r dev",
    "start:dev": "pnpm --stream --pararel -r run start:dev",
    "build": "pnpm --stream -r run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
